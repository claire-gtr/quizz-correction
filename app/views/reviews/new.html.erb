<div class="container">
  <div class="row">
    <div class="col">
      <%= simple_form_for [@song, @review] do |r| %>
        <%= r.input :content %>
        <%= r.button :submit, class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
